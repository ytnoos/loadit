plugins {
    id("loadit-publish")
}

dependencies {
    api(project(":api"))
    api(project(":bukkit-common"))
    implementation(project(":bukkit-legacy"))
    implementation(project(":bukkit-modern"))

    compileOnly(libs.spigot)
}