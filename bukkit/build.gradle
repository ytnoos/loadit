plugins {
    id("loadit-publish")
}

dependencies {
    implementation(project(":api"))

    compileOnlyApi(libs.spigot)
}

tasks.shadowJar {
    archiveBaseName.set("loadit-bukkit")
    archiveClassifier.set("")
}